# Anthropic API Key — starts with sk-ant-
# Found in: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# GitHub Personal Access Token — needs 'repo' scope
# Create at: https://github.com/settings/tokens → Generate new token (classic) → check 'repo'
GITHUB_TOKEN=ghp-your-token-here

# GitHub Organization name — repos will be created here instead of your personal account
# Leave blank to create repos under your personal account
GITHUB_ORG=your-org-name-here

# Railway API Token — must be an account-level token (not a project token)
# Create at: https://railway.com/account/tokens
RAILWAY_TOKEN=your-railway-token-here

# GitHub token with delete_repo scope — used only by cleanup_deploy.py
# Create a separate token at: https://github.com/settings/tokens
GITHUB_TOKEN_CLEANUP=ghp-your-cleanup-token-here

# Persistent storage directory for sessions and solutions registry
# Local: leave blank or set to your project root (defaults to project root)
# Railway: set to /data (Railway Volume mount point)
DATA_DIR=

# Storage backend: "sqlite" (default, zero setup) or "supabase" (production)
STORAGE_BACKEND=sqlite

# Supabase — required when STORAGE_BACKEND=supabase
# Project URL: https://supabase.com/dashboard → project → Settings → API → Project URL
SUPABASE_URL=
# Service role key (NOT anon key — needs write access): Settings → API → service_role
SUPABASE_KEY=

# Email notifications (optional) — used in auto mode to email when demo is done
# Create a free API key at: https://resend.com
RESEND_API_KEY=re_your-key-here

# Admin API token — protects /_admin/* endpoints (DELETE /admin/sessions, etc.)
# Set to any strong secret string. If not set, admin endpoints return 500.
# curl usage: -H "Authorization: Bearer <token>"
ADMIN_TOKEN=your-strong-secret-here

# (Optional) Dedicated key for deployed demos. If empty, falls back to ANTHROPIC_API_KEY
DEMO_ANTHROPIC_API_KEY=

# (Optional) Specify which model the deployed demos should use (cheaper for runtime).
# Defaults to claude-3-haiku-20240307 if not specified.
DEMO_ANTHROPIC_MODEL=claude-3-haiku-20240307

# (Optional) Specify which model the pipeline uses to build the demos (requires strong reasoning).
# Defaults to claude-sonnet-4-20250514 if not specified.
BUILDER_ANTHROPIC_MODEL=claude-sonnet-4-20250514
